"""MODE:P vNext — Schema definitions.

This sub-package will hold all vNext data schemas (JSON Schema, Canonical
Serialization contracts, Manifest formats, etc.). Currently a placeholder;
schemas are added incrementally by later tasks (V1.x, V2.x, etc.).
"""

# Schema package version follows the parent package.
