# MODE:P 快速参考

## 输入

Scene Context：剧本、空间事实、人物连续性、参考材料、上一场终态与用户意图。

## 调用

`Director -> DP -> Director revise (when needed) -> SD2.0 preflight -> delivery`

## 交付

- `STORYBOARD_[script]_[scene].md`
- `VIDEO_PROMPT_[script]_[scene].md`

## 禁止引入

多领域设计 Agent、YAML、JSON 作为 Agent 接口、复杂度路由、TIME_SKELETON、PLAN、Gate 0、审计报告、KB 规则 ID、Seko 平台语法。
